import { StringDateFormatterPipe } from './string-date-formatter.pipe';

describe('StringDateFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new StringDateFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
